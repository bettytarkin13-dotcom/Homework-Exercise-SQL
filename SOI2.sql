SELECT*
FROM STUDENTS;

SELECT NAME,GRADE
FROM STUDENTS;

SELECT*
FROM STUDENTS
WHERE GRADE > 90;

SELECT*
FROM STUDENTS
WHERE NAME like 'M%';

SELECT
    avg(GRADE),
	  max(GRADE),
	  min(GRADE)
FROM STUDENTS;

SELECT*
FROM STUDENTS
WHERE GRADE > (SELECT avg(GRADE) FROM STUDENTS);

UPDATE STUDENTS
SET GRADE = 90
WHERE NAME = 'Liam';


